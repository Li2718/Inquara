{
  "name": "inquara",
  "private": true,
  "engines": {
    "node": ">=24.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "node --env-file=.env scripts/dev.mjs",
    "dev:infra:up": "node --env-file=.env scripts/dev.mjs infra-up",
    "dev:infra:down": "node --env-file=.env scripts/dev.mjs infra-down",
    "dev:start": "node --env-file=.env scripts/dev.mjs start",
    "dev:stop": "node --env-file=.env scripts/dev.mjs stop",
    "dev:api": "node --env-file=.env scripts/run-workspace.mjs @inquara/api dev",
    "dev:web": "node --env-file=.env scripts/run-workspace.mjs @inquara/web dev",
    "build": "npm run build --workspaces",
    "lint": "eslint . --cache --cache-location ./.cache/eslint/",
    "lint:fix": "eslint . --fix --cache --cache-location ./.cache/eslint/",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify:debug-free": "node scripts/verify-debug-free.mjs",
    "db:generate": "npm --workspace @inquara/db run generate",
    "db:migrate": "npm --workspace @inquara/db run migrate",
    "db:migrate:deploy": "npm --workspace @inquara/db run migrate:deploy"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@next/eslint-plugin-next": "^16.2.6",
    "@types/node": "^22.0.0",
    "@types/pg": "^8.20.0",
    "dotenv": "^16.6.1",
    "eslint": "^10.4.0",
    "globals": "^17.6.0",
    "npm-run-all": "^4.1.5",
    "pg": "^8.21.0",
    "testcontainers": "^12.0.0",
    "typescript": "^5.8.0",
    "typescript-eslint": "^8.60.0",
    "vitest": "^3.0.0"
  }
}
